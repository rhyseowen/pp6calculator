/**
 * @file weekOneMenu.hpp
 * @brief Menu of opperations for week 1
 */
#ifndef WEEKONEMENU_HH
#define WEEKONEMENU_HH 

/**
 * @brief Main week one menu loop
 * @return Error code
 */
int weekOneMenu();

/**
 * @brief Print help message for week one
 */
void weekOnePrintHelp();

/**
 * @brief Addition menu opperation
 * @return Error code
 */
int wk1_addition();

/**
 * @brief Subtraction menu opperation
 * @return Error code
 */
int wk1_subtraction();

/**
 * @brief Division menu opperation
 * @return Error code
 */
int wk1_division();

/**
 * @brief Multiplication menu opperation
 * @return Error code
 */
int wk1_multiplication();

/**
 * @brief Solve axis menu opperation
 * @return Error code
 */
int wk1_solveAxis();

/**
 * @brief Solve quadratic opperation
 * @return Error code
 */
int wk1_solveQuadratic();

/**
 * @brief 3 vector length opperation
 * @return Error code
 */
int wk1_3VectorLength();

/**
 * @brief 4 vector length opperation
 * @return Error code
 */
int wk1_4VectorLength();

/**
 * @brief Invarient mass menu opperation
 * @return Error code
 */
int wk1_invarientMass();

#endif
